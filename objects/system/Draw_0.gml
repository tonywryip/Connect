/// @desc <>

/*
//temp code
for (var xx = 0; xx < ds_grid_width(global.grid); xx++) {
	for (var yy = 0; yy < ds_grid_height(global.grid); yy++) {
		var _ind = ds_grid_get(global.grid, xx, yy);
		if ds_grid_get(global.grid, xx, yy) != 0 {
			draw_sprite(s_ds_grid, _ind - 1, xx * cell_size + grid_xoffset, yy * cell_size + grid_yoffset);
		}
	}
}
*/